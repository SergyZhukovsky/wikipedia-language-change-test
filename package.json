{
  "name": "wikipedia-language-change-test",
  "version": "1.0.0",
  "description": "Automated test for Wikipedia language change functionality",
  "scripts": {
    "test": "npx playwright test --config config/playwright.config.ts",
    "test:headed": "npx playwright test --config config/playwright.config.ts --headed",
    "report": "npx playwright show-report"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "@types/node": "^25.3.0"
  },
  "private": true
}