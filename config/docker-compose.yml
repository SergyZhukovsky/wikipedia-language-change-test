services:
  tests:
    build:
      context: ..
      dockerfile: config/Dockerfile
    env_file: ../.env
    volumes:
      - ../playwright-report:/app/playwright-report
      - ../test-results:/app/test-results